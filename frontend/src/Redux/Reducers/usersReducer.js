const usersInitialState = {
    data:[]
}

const usersReducer = (state=usersInitialState, action)=>{
    switch (action.type){
        

        default:{
            return {...state}
        }
    }
}

export default usersReducer